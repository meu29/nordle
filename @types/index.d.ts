type WordleTheme = {
    id: string;
    content: string;
    content_furigana: string;
    tag: string;
}

type LetterColor = {
    color: string;
    bg: string;
}

type Restaurant = {
    name: string;
    address: string;
}